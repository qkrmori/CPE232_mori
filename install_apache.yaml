---
  - hosts: all
    become: true
    tasks:
    
    - name: install apache2 & php package Ubuntu
      package:
        name:
          - "{{ apache_package }}"
          - "{{ php_package }}"
        state: latest
        update_cache: yes